# Clôtures opérationnelles — 2026-08-29

Synthèse des tâches **closes** ce jour. Pas le canon input. Pas un registre de lots à exécuter. Pas une entrée de version.

Registre : [`dev/PLAN.md`](../../dev/PLAN.md).  
Canon input : [`docs/governance/citadelle-input-invariants.md`](../governance/citadelle-input-invariants.md).  
B/C/D disque `models/` : **non ouvertes**.

Branche : `wip/checkpoint-20260824-0117`. Pas de push.

---

## 1. Lot modèles / disque — étape A close

Objectif : retirer deux GGUF locaux redondants. Le runtime parle à Ollama par **tags**, pas par `models/*.gguf`.

- Gain constaté : **12 850 211 008 octets** (~12,85 Go).
- Supprimés (hors Git, déjà gitignorés) : `Qwen2.5-Coder-7B-Instruct-Q6_K.gguf`, `Meta-Llama-3.1-8B-Instruct-Q6_K.gguf`.
- Runtime inchangé. Store Ollama inchangé.
- Restent dans `models/` : GGUF vox + `Modelfile-vox` / `.qwen` / `.llama`.
- Étapes B, C, D : **non ouvertes**. Reprise seulement sur GO explicite.

Risque résiduel : reproductibilité des variantes Q6 (re-téléchargement si `ollama create` plus tard). Pas de risque runtime identifié.

---

## 2. Correctif `conversation_recall` — close

Commit : `4437777` — `fix(agent): harden conversation recall against inline documents`.

- Faux positif : page HTML collée (CTA « Discuter » + « avant tout ») ne coupe plus vers le rail rappel.
- Garde durcie sur documents / fences collés et longs ; fenêtres prefix/suffix séparées.
- Replay portfolio : `isConversationMemoryRecallRequest = false` ; le rappel ne coupe plus le chemin HTML / build.
- Patch recall **non** fusionné avec le triage `code_explain`.

---

## 3. Correctif `code_explain` (payload collé) — close

Lot registre : `CODE_EXPLAIN_PASTED_PAYLOAD`. Commit : `21d2ad8` — `fix(agent): ignore pasted payload in code explain triage`.

- Faux positif triage : mot du copy HTML (`pédagogique`) ne classe plus la consigne en `code_explain`.
- Scan lexical limité à la **consigne** (prefix avant document/fence).
- Portfolio exact : plus de suffixe console **`triage : Explication`**.
- JUST inchangé sur le cas validé : `web_html` / `structure` / `build_v1` (high). Consume JUST toujours gelé.

---

## Hors cette synthèse

Snapshot **2026-08-29** seulement. Lots du 30–31 août (HTML inline, social/meta, calendar, architecture, cleanup, README) : [`dev/PLAN.md`](../../dev/PLAN.md).  
File GO1→GO6 : déjà close au registre.  
Journaux de version (`Journal_de_bord.md`, `Journal_des_ameliorations.md`) : pas d’entrée — pas de GO « version ».
