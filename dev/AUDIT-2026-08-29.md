# Audit HEAD — 2026-08-29

Snapshot lecture seule. **Pas le canon.** Pas un GO. Pas un registre de lots.

Canon input : `docs/governance/citadelle-input-invariants.md`  
Registre : `dev/PLAN.md`  
Périmètre lu : routing, guards, pipelines, tests, docs, file GO1→GO6.

Date : 2026-08-29. Branche : `wip/checkpoint-20260824-0117`.

---

## Résumé

File GO1→GO6 **terminée et cohérente** sur HEAD (`86e8d8f`, `ea49d42`, `038cbf3`).

Risque principal **actuel** : working tree **sale** (WIP hors scope), pas la base HEAD.

Risque technique **important** (dette, pas un bug file) : god-modules
`intentShortCircuit.js` (~3000 lignes), `agentPipeline.js` (~4100),
`informationSeekingIntentGuards.js` (info-seeking + création textuelle, 30+ importateurs).

---

## Priorités

| P | Point | Reco |
|---|--------|------|
| P0 | WIP `D server/src/agent/utils/*`, experts JSON, FEEDBACK, tests social dirty | Isoler. Jamais `git add` large. |
| P1 | `isExplicitTextCreationRequest` collé dans info-seeking ; `isInformationSeekingWithTarget(query)` **sans** `history` | Isoler plus tard (lot D). Ne pas rouvrir GO1–GO6. |
| P1 | PDF FILE_ANALYSIS : `onContent: null` dans `agentPipeline.js` (~L3066) ; tests unité seulement | Filet intégration (lot C). |
| P1 | `onContent: null` polysémique (orchestrateur / PDF / SIMPLE_FAST) | Accepter comportement ; nommer si lot minuscule. |
| P1 | SC / pipeline god-files ; ordre SC = contrat | Accepter. Pas de split maintenant. |
| P2 | `isSubstantiveWorkRequest` n’inclut pas poème/conte (résidu D0) | Accepter. |
| P2 | PLAN : libellés « Non ouverts D1–D5 » / « 2 commits » | Corrigé au registre (`D1–D5 clos/skip`, 3 commits). |
| P2 | SoT branche `@ 5c9d682` ; ROADMAP / CITADEL_V3–V4 marketing | Pointer, ne pas traiter comme vérité runtime. |
| P2 | Regex learning miroir ; heuristique D2 sujet/genre étroit | Accepter. |
| P2 | JUST shadow ; rouges gelés canon | Accepter. Gel. |

---

## Risques

- Push / PR avec WIP = imports cassés.
- Changer `informationSeekingIntentGuards.js` = régression croisée social / web / clarify / SC.
- Régression buffer PDF invisible (pas d’assert pipeline `onContent`).
- Docs `ROADMAP.md`, `CITADEL_V3_OPERATIONAL.md`, `CITADEL_V4_OPERATIONAL.md` : **pas** source de comportement.
- `docs/governance/documentation-source-of-truth.md` : restore checkpoint ; **pas** la base C2 ; HEAD file ≠ `5c9d682`.
- Tests gelés (`citadelle-input-invariants.md` § Rouges gelés) : ne pas « réparer au passage ».

---

## Ne pas toucher maintenant

C2 / `npm ci`, consume JUST, `entities`, export PDF fichier, SQL livré, D4, D0, GO1–GO6.  
Rouges gelés. Split SC / pipeline. Suppressions `utils/*` WIP.  
Élargir `TEXT_CREATION_GENRE_RE`. Framing / `conversationTurnClassifier` dirty.

---

## Lots proposés (non ouverts)

Pas un GO. Ouvrir seulement sur nommage + fiche 5 champs.

| Id | Nom | Contenu |
|----|-----|---------|
| A | `GIT_WIP_ISOLATE` | Stage path-by-path. WIP hors PR. |
| B | `PLAN_REGISTRY_HYGIENE` | Registre : libellés faux seulement. |
| C | `PDF_PIPELINE_BUFFER_TEST` | Test `onContent: null` PDF FILE_ANALYSIS. 0 métier. |
| D | `TEXT_CREATION_GUARD_SPLIT` | Extraire guards text_creation + `history` sur preempts. |
| E | `DOC_SOT_POINTER` | Pointer SoT commit/branche actuel sans en faire la base C2. |

Ordre : **A avant tout push**. B/C sans rouvrir GO. D après merge + GO. E au merge.

---

## Preuve audit

Batterie file : 79 pass / 0 fail (revue transversale).  
Commits file : GO1+GO2 ; D3+D1+D2+D5 ; `docs(plan): clôture file GO1→GO6`.
