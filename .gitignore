# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/
playwright-report/
test-results/

# Production / Build
dist/
build/
.cache/

# Environment Variables
.env
.env.*
!.env.example
!**/.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Server specifics
server/node_modules/
server/.env

# Async Forge scratch workspaces (may contain copied .env secrets)
scratch/

# Nexxus Projects (Physical generated files)
projects/
server/data/experts_cache.json
server/cache/experts_cache.json
server/cache/workspace_index.json
server/data/reports/
server/data/sessions/
server/state/sessions/
server/state/session-work-memory/
server/data/session_owners.json
docs/archive/session_owners.json
server/data/security/audit-history.jsonl
server/data/telemetry/*.json
server/data/video-uploads/
server/data/video-jobs/
## Browser Harness (sessions observées)
server/src/data/browser-sessions/
## Pipeline Design Extract (artefacts jobs)
server/src/data/design-extract-jobs/
server/data/design-extract-jobs/

# Nexxus LLM Temp & Cache
.cortex/
.gemini/
*.log

# Python / local ML runtimes (Chroma, AirLLM, creative)
.venv/
venv/
**/__pycache__/
*.pyc

# Graphify — graphes régénérables (lourds)
**/graphify-out/

# Vector DB locale + modèles GGUF (purge vague 3 — arbitrage humain)
server/data/chroma/
models/

# Forge async outputs (artefacts jobs)
tools/async_forge/outputs/

# Citadelle vault — journaux de tours (volume + vie privée, hors Git)
citadelle-vault/**/01-Episodic/interactions/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
logs/

# Editor / System files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store
Thumbs.db

# Documentation / Memory (Keeping them for now as they are project assets)
#! .memory/
#! docs/
